apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: bc-eks
  region: eu-west-3
  version: "1.30"

managedNodeGroups:
  - name: ng-app
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 5
    privateNetworking: false
